module tb;
reg clk;
reg rst;
wire baud_tick;
baud_gen dut(
    .clk(clk),
    .rst(rst),
    .baud_tick(baud_tick));
initial begin
    clk = 0;
    forever #5 clk = ~clk;
end
initial begin
    rst = 1;
    #20 rst = 0;
    #1000;
    $finish;
end
endmodule
